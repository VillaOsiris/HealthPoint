const CTA = () => {
  return (
    <div className="cta">
      <h2 className="cta__text">Try 1 month free with Season Pass.</h2>
      <button className="cta__button">Subscribe now!</button>
    </div>
  );
};

export default CTA;
